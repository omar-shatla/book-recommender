# Google AI Studio API Key (optional — used only in early experimentation, not required by gradio-dashboard.py)
GOOGLE_API_KEY=your_key_here

# HuggingFace token (optional — used only if switching to a gated/private HF model)
HF_TOKEN=your_token_here
